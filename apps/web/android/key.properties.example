# Copy to key.properties (same folder) — do not commit key.properties or the .jks file.
# Paths are relative to apps/web/android/

storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=stonesummoner
storeFile=stonesummoner-upload.jks
